using UnityEngine;
using System.Collections;

public class SpriteFront : MonoBehaviour
{
    // Update is called once per frame
    void Update()
    {
        transform.rotation = Quaternion.identity;
    }
}